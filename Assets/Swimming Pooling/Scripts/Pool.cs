using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Pooling {
    public class Pool : MonoBehaviour {
        /*public int amount;
        PooledItem[] items;

        void Start(){
            // Create array of items
            items = new PooledItem[amount];

            // Create pooled items
            for (int i = 0; i < items.length; i++){
                GameObject obj = Instantiate(
                    new GameObject("Pooled-" + this.gameObject.name + "-" + i)
                );

                items[i] = new PooledItem(obj);
            }
        }

        void Update(){
            foreach (var item in items){
                item.Update();
            }
        }

        public void ChangeObjects<T>(int amount, T x){
            for (int i = 0; i < amount; i++){
                
            }
        }*/
    }
}